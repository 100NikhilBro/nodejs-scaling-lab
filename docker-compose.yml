services:

  app:
    build: .
    container_name: node-app

  nginx:
    image: nginx:latest
    container_name: nginx-proxy

    ports:
      - "8080:80"

    volumes:
      - ./conf/nginx.conf:/etc/nginx/nginx.conf

    depends_on:
      - app